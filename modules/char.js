export default class Character {
  constructor(hp, armor, dmgModifier, resisits, mp, handSize, effects) {
    this.hp = hp;
    this.armor = armor;
    this.dmgModifier = dmgModifier;
    this.resisits = resisits;
    this.mp = mp;
    this.handSize = handSize;
    this.effects = effects;
  }
}
